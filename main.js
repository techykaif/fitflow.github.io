// Main JavaScript file to include the sidebar in all pages
import { initSidebar } from "./components/sidebar.js"

// Initialize the sidebar when the DOM is loaded
document.addEventListener("DOMContentLoaded", () => {
  initSidebar()
})

